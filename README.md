# React [Vite](https://vitejs.dev/guide/) Boilerplate

## Before start

Upgrade versions of all dependencies in `package.json`.


## Install packages

```bash
  $ yarn install
```

## Run

```bash
  $ yarn dev
```
The project will be available in `localhost:3000`